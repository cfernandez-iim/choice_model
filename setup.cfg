[metadata]
name = IIM_core_model
version = 0.1.0
description = My Python application

[options]
packages = find:
python_requires = >=3.10
install_requires =
    pyyaml
    pandas
    numpy
    scipy

[options.entry_points]
console_scripts =
    IIM_model = IIM_core_model.main:main